"""
Module for the Base schema, to provide additional functionality for all schemas.
"""

from marshmallow import Schema


class Base(Schema):
    """
    Base schema for all schemas to inherit from.
    """
